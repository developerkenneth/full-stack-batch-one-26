// Maths Object

// random
let result ;


// generate random numbers from 0.00 to 1
result = Math.random();

// generate custom random number from 0.00 to 100
result*=100;

// round float numbers to the nearest integers
result = Math.round(result);


const float = 67.8;
const floatTwo = 48.3;

result = Math.round(floatTwo);

// ceil takes the float to the nearest hightest whole number
result = Math.ceil(floatTwo);

// ceil takes the float to the nearest hightest whole number
result = Math.floor(floatTwo);

console.log(result);
